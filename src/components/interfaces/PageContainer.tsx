import { styled } from 'styled-components';

const PageContainer = styled.div`
    width: 80vw;
    margin: 0 auto;
    background-image: url(../../../public/floral.jpg);
`;

export default PageContainer;